// Archivos subidos por el usuario en `src/assets`:
// - 3dias.png          (flamencos — 3 días)
// - 4diasClasica.png   (ruta clásica — 4 días)
// - 4diasInversa.png   (ruta inversa — 4 días)

import tresDias from './3dias.png';
import clasica from './4diasClasica.png';
import inversa from './4diasInversa.png';

// Si no hay imágenes separadas del Salar, reutilizamos las imágenes disponibles
const salar1 = clasica;
const salar2 = inversa;
const salar3 = tresDias;

export { tresDias, clasica, inversa, salar1, salar2, salar3 };
